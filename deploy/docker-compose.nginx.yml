version: "3.9"

services:
  nginx:
    image: nginx:1.27-alpine
    container_name: smart-paper-nginx
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx/smart-paper.conf:/etc/nginx/conf.d/default.conf:ro
    network_mode: host
